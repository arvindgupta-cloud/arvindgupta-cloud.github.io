name: Deploy to Netlify

on:
  push:
    branches:
      - main

permissions:
  contents: write
  statuses: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy to Netlify
        run: |
          curl -H "Authorization: Bearer ${{ secrets.NETLIFY_AUTH_TOKEN }}" \
          -X POST -F file=@./index.html \
          https://api.netlify.com/api/v1/sites/60030752-132a-4d76-a04d-d5e71fc87e71/deploys

      - name: Print Deployed URL
        run: |
          echo "Your website is live at: https://uniqueportpolio.netlify.app"

      - name: Update Commit Status with URL
        uses: actions/github-script@v6
        with:
          script: |
            github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: context.sha,
              state: "success",
              description: "Website successfully deployed!",
              context: "Netlify Deployment",
              target_url: "https://uniqueportpolio.netlify.app"
            });

      - name: Post Comment with Deployed URL
        uses: actions/github-script@v6
        with:
          script: |
            github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.run_id, # Using run ID as fallback for non-PR workflows
              body: `✅ Your website is live at: [https://uniqueportpolio.netlify.app](https://uniqueportpolio.netlify.app)`
            });
